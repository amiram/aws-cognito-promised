{
  "name": "aws-cognito-promised",
  "version": "0.0.6",
  "description": "",
  "main": "lib/index.js",
  "typings": "lib/index.d.ts",
  "scripts": {
    "lint": "tslint \"src/**/*.ts\"",
    "build": "tsc",
    "precommit": "npm run lint && npm run build",
    "prepublish": "npm run lint && npm run build"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "amazon-cognito-identity-js": "^2.0.1",
    "bluebird": "^3.5.1"
  },
  "devDependencies": {
    "@types/bluebird": "^3.5.20",
    "@types/node": "^8.0.0",
    "husky": "^0.14.3",
    "tslint": "^5.9.1",
    "typescript": "^2.7.2"
  }
}
